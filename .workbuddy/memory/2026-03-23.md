# 2026-03-23

## 模块初始化 — brosdk-sdk-typescript

- 分析了 `brosdk.ts`：通过 `koffi` 调用 Windows/macOS 原生动态库的 TypeScript class，封装了浏览器 SDK（init、env 管理、browser 操作、回调注册等）。
- 将目录初始化为 npm 模块，步骤如下：
  - 创建 `package.json`（包名 `brosdk-sdk`，入口 `dist/index.js`，类型 `dist/index.d.ts`，依赖 koffi，peerDep electron）
  - 创建 `tsconfig.json`（target ES2020，commonjs，输出 dist，生成 .d.ts）
  - 创建 `.gitignore` 和 `.npmignore`
  - 将 `brosdk.ts` 移入 `src/` 目录
  - 创建 `src/index.ts` 作为统一导出入口
  - `npm install` 安装依赖（46 packages）
  - `npm run build` 编译成功，dist 目录产物：`index.js`、`brosdk.js` 及对应 `.d.ts`、`.map` 文件

## 修复 process.resourcesPath 类型错误
- `process.resourcesPath` 是 Electron 注入的属性，`@types/node` 无此定义
- 解决方案：将 `electron` 加入 `devDependencies`，其自带类型扩展了 `NodeJS.Process` 接口
- `tsc --noEmit` 零错误

## 编写 README.md
- 编写了完整的 README，包含：安装、动态库目录结构、快速上手、Electron IPC 完整示例、API 参考表、错误码工具、内存管理注意事项、electron-builder 打包配置说明

## Git 提交与推送
- 首次提交 `f71a976`：初始化模块（9 个文件）
- 第二次提交 `377cc2d`：添加 `prepare` 脚本，支持从 GitHub 直接安装时自动构建
- 推送到 `https://github.com/browsersdk/brosdk-sdk-typescript.git`

## 发布方式说明
- 方案一（推荐）：发布到 npm registry，用户执行 `npm install brosdk-sdk`
- 方案二：直接从 GitHub 安装，用户执行 `npm install browsersdk/brosdk-sdk-typescript`
  - 已添加 `prepare` 脚本，安装时会自动运行 `npm run build` 生成 `dist/`
